using AOCHelper;

namespace {0};

public class Day{1}(int year) : AOC_DayBase(year, {2})
{{
    protected override void SolveA_Internal(string input)
    {{
        AOC_Logger.Display("Solving A!");
    }}

    protected override void SolveB_Internal(string input)
    {{
        AOC_Logger.Display("Solving B!");
    }}

    protected override string TestInput => "";
}}
